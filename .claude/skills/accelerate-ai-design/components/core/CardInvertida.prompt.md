Card invertida sobre fondo claro.

```jsx
<CardInvertida style={{marginRight:-40}}>
  <div className="t-gigante" style={{color:"var(--cyan-300)"}}>68%</div>
  <div className="t-label" style={{color:"var(--text-body-light)"}}>de las consultas se resuelven sin humano</div>
</CardInvertida>
```

Nunca dos invertidas en la misma pieza. Dale profundidad: que algo la cruce (un Chip montado, o que la card salga del margen).
