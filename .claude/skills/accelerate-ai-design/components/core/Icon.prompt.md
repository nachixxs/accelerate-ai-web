Ícono de Lucide, la única familia del sistema.

```jsx
<Icon name="chevron-right" size={48} color="var(--text-heading-light)" style={{opacity:.6}} />
<Icon name="clock" size={56} color="var(--brand-primary-deep)" />
```

No dibuges íconos a mano. Si el concepto no existe en Lucide, cambiá el concepto.
